# jackpro-release

JACKPRO 插件的**更新发布仓库**。

## 目录内容

- `package.json`：`version` 字段代表最新版本号，插件据此判断是否有新版本。
- `jackpro-<版本>.vsix`：对应版本的插件安装包，文件名带版本号便于自动挑选最新包。

## 发布新版本流程

1. 提高 `package.json` 中的 `version`（如 `1.0.60`）。
2. 把新的 `jackpro-1.0.60.vsix` 放入本仓库根目录。
3. `git add . && git commit -m "release 1.0.60" && git push`。

客户端插件（侧边栏「自动更新 (Git 远程)」填入本仓库地址并开启自动更新）会在检查时：
克隆/拉取本仓库 → 比对 `package.json` 的 `version` → 若更高则安装根目录中版本号最高的 `.vsix` → 提示重启生效。

## 当前版本

- **1.0.72**
  - 修复：后台自动检测到新版本时不弹窗（需重新打开面板才显示）——面板隐藏时保留上下文，保证后台更新提示送达。
  - 调整：默认开启自动更新。
- 1.0.71
  - 新增：点击侧边栏底部「版本」卡片即可手动检查更新；结果以面板内 toast 提示。
- 1.0.70
  - 修复：自动值守/更新后悬浮球帧动画丢失——帧目录改为始终取当前安装目录下的 001，不再沿用配置里的旧版本绝对路径。
  - 调整：「运行」卡片中描述文字移到快捷键行下方。
- 1.0.69
  - 修复：更新弹窗因 CSS 层叠问题常驻显示（只显示占位「v-」）——改用 `.show` 类控制显隐。
  - 修复：点击「稍后提醒」不关闭弹窗；现可正常关闭并在 1 分钟后再次提醒。
  - 已是最新版本时不再弹出更新窗口；版本号完整显示不换行。
- 1.0.68
  - 更新提示改为直接显示在插件侧边栏面板内（面板同款深色 UI），不再打开独立编辑器标签页。
  - 移除界面与自述文件中的全部 emoji 图标。
  - 修复：桌面悬浮球帧动画偶发消失——帧集合加载成功后原子替换，加载失败保留现有帧并稍后重试。
- 1.0.67
  - 去掉运行卡片中双击悬浮球提示前的 emoji 图标。
- 1.0.66
  - 更新提示改为插件自定义弹窗（Webview 面板），不再是 VSCode 原生通知。
  - 实时检测更新：每 1 分钟检查一次；用户暂不更新时，每 1 分钟再次弹窗提醒。
